# Supabase project credentials (Supabase Dashboard → Settings → API)
# Used by @supabase/ssr for auth + database access. NEVER commit real values.



# Optional: used only if you later add a seed/admin script with the service role.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
